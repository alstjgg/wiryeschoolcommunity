# Core
chainlit==2.10.0
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-anthropic>=1.0,<2.0
langgraph>=1.0,<2.0

# Google API
google-api-python-client==2.166.0
google-auth==2.38.0

# Excel processing
openpyxl==3.1.5
xlrd==2.0.1

# HTML parsing (배움숲 신청자 목록)
beautifulsoup4==4.12.3

# PDF generation (출석부 인쇄용)
reportlab==4.2.5

# Utilities
python-dotenv==1.1.0

# PostgreSQL (채팅 기록 + 비즈니스 데이터)
asyncpg==0.30.0
langgraph-checkpoint-postgres>=2.0
psycopg[binary]>=3.1

# HTTP client (anthropic SDK 의존)
httpx>=0.27.0

# Observability (LangSmith tracing)
langsmith>=0.3
